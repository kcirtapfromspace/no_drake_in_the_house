# Development-specific values for ndith
# Override default values for local development

backend:
  image:
    repository: ndith/backend
    tag: latest
    pullPolicy: Never  # Use local images
  
  env:
    DATABASE_URL: "postgres://ndith:password@ndith-postgresql:5432/ndith"
    REDIS_URL: "redis://ndith-redis-master:6379"
    JWT_SECRET: "dev_jwt_secret_change_in_production"
    RUST_LOG: "debug"
    TOTP_ISSUER: "No Drake in the House (Dev)"
  
  resources:
    requests:
      memory: "256Mi"
      cpu: "100m"
    limits:
      memory: "512Mi"
      cpu: "500m"

frontend:
  image:
    repository: ndith/frontend
    tag: latest
    pullPolicy: Never  # Use local images
  
  env:
    VITE_API_URL: "http://localhost:3000"
  
  resources:
    requests:
      memory: "128Mi"
      cpu: "50m"
    limits:
      memory: "256Mi"
      cpu: "200m"

postgresql:
  enabled: true
  auth:
    username: ndith
    password: password
    database: ndith
  primary:
    persistence:
      enabled: false  # Use ephemeral storage for dev
    resources:
      requests:
        memory: "128Mi"
        cpu: "50m"
      limits:
        memory: "256Mi"
        cpu: "200m"

redis:
  enabled: true
  auth:
    enabled: false
  master:
    persistence:
      enabled: false  # Use ephemeral storage for dev
    resources:
      requests:
        memory: "64Mi"
        cpu: "25m"
      limits:
        memory: "128Mi"
        cpu: "100m"

# Enable ingress for local development
ingress:
  enabled: true
  className: "nginx"
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
  hosts:
    - host: ndith.local
      paths:
        - path: /api
          pathType: Prefix
          service: backend
        - path: /
          pathType: Prefix
          service: frontend