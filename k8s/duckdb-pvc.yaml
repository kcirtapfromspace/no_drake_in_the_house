apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ndith-duckdb-data
  namespace: ndith-production
  labels:
    app: ndith-api
    component: analytics-storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: fast-ssd
