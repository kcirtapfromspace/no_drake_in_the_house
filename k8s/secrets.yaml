apiVersion: v1
kind: Secret
metadata:
  name: ndith-secrets
  namespace: ndith-production
type: Opaque
# TEMPLATE FILE — do not add real values here.
# Copy to secrets.local.yaml (gitignored) and fill in real values, OR
# use ExternalSecrets / SealedSecrets / SOPS to manage production secrets.
#
# To create the Secret from a local file:
#   kubectl apply -f secrets.local.yaml
#
# See docs/secrets-management.md for setup instructions.
stringData:
  DATABASE_URL: ""
  REDIS_URL: ""
  JWT_SECRET: ""
  KMS_KEY_ID: ""
  SPOTIFY_CLIENT_SECRET: ""
  APPLE_MUSIC_PRIVATE_KEY: ""
