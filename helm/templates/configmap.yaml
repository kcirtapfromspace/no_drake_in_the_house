{{- if .Values.configMap.create }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ndith.fullname" . }}-config
  labels:
    {{- include "ndith.labels" . | nindent 4 }}
data:
  RUST_LOG: {{ .Values.backend.env.RUST_LOG | quote }}
  TOTP_ISSUER: {{ .Values.backend.env.TOTP_ISSUER | quote }}
{{- end }}